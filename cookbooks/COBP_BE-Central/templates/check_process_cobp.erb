#!/bin/bash
ps -ef | grep "/var/tellme/cobp/" | grep -v "grep"
returnCode=$?
if [ ${returnCode} -eq 0 ]
then
        echo "COBP is running"
        exitCode=${returnCode}
else
        echo "COBP is stopped"
        exitCode=2
fi
exit ${exitCode}