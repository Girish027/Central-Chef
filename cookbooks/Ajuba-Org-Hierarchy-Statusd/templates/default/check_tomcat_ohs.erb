#!/bin/bash
ps -ef | grep "/var/tellme/OHS/tomcat/" | grep -v "grep"
returnCode=$?
if [ ${returnCode} -eq 0 ]
then
        echo "Tomcat(OHS) is running"
        exitCode=${returnCode}
else
        echo "Tomcat(OHS) is stopped"
        exitCode=2
fi
exit ${exitCode}